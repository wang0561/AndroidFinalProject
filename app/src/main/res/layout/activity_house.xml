<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.algonquin.cst2335final.HouseActivity">

    <ListView
        android:layout_height="match_parent"
        android:layout_width="@dimen/House_width_left"
        android:layout_alignParentStart="true"
        android:id="@+id/HouseListView"
        android:layout_above="@+id/HouseButton" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/HouseEditText"
        android:hint="@string/HouseHintString"

        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true" />

    <Button
        android:layout_height="wrap_content"
        android:layout_width="@dimen/House_width_left"
        android:id="@+id/HouseButton"
        android:text="@string/confirmB"
        android:layout_above="@+id/HouseEditText"
        android:layout_alignParentStart="true" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/HouseFrameLayout"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/HouseListView"
        android:layout_above="@+id/HouseEditText">

    </FrameLayout>


</RelativeLayout>
