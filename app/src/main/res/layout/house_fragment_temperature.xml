<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:id="@+id/house_fragment_temperature">
    <Button
        android:text="@string/houseAddButton"
        android:layout_width="60dp"
        android:layout_height="wrap_content"
        android:id="@+id/houseAddButton"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true" />

    <ListView
        android:layout_width="135dp"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_above="@id/houseAddButton"
        android:layout_alignEnd="@+id/houseDeleteButton"
        android:id="@+id/houseTempListView"/>


    <Button
        android:layout_width="75dp"
        android:layout_height="wrap_content"
        android:id="@+id/houseDeleteButton"
        android:text="@string/houseDelButton"
        android:layout_alignParentBottom="true"
        android:layout_toEndOf="@+id/houseAddButton" />

    <FrameLayout
        android:id="@+id/numberPickerFrameLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_toEndOf="@+id/houseTempListView">

    </FrameLayout>
</RelativeLayout>